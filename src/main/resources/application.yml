---
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yamasunquest/spring-cloud-configserver

info:
  name: com-sunk-springCloud-configServer
  version: 1.0